spring:
  kakao:
    auth:
      client: d0e33acc669d3d7994242e6879cefb32
      redirect: http://localhost:8080/auth/login/kakao

  data:
    redis:
      host: localhost
      port: 6379


jwt:
  secret-key: fxZIpib8a0swUhizWyuqA2+EkGWoVcisX91kVYqlvHo=

#  datasource:
#    url: jdbc:mysql://localhost:3306/testdb
#    username: root
#    password: 1234
#    driver-class-name: com.mysql.cj.jdbc.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true
#    database-platform: org.hibernate.dialect.MySQL8Dialect
datasource:
  url: jdbc:h2:mem:testdb  # H2 인메모리 데이터베이스 사용
  driver-class-name: org.h2.Driver
  username: sa
  password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update  # 자동 테이블 생성
    show-sql: true  # 실행되는 SQL 쿼리 출력
  h2:
    console:
      enabled: true  # H2 콘솔 활성화
      path: /h2-console  # 웹 콘솔 접속 경로